Aim for simplicity when writing components and functions.

- Reduce the amount of coupling, we should expect arguments over specific object types unless the class name
  specifically wants that
- Use values and other abstractions in your functions
- Resist mixing in with the "How". Separating the "What does this do?" from "How does it do it?" is important.
